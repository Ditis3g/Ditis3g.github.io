I've hit a real blocker, and I don't want to paper over it by inventing content.

**What happened**

The study notes in your message arrived mojibaked — every Korean character is rendered as `?` (e.g. the title came through as `# ??? AI, ?? ??? ??? ?? "??"...`). That corruption is lossy: once Korean → `?`, the original words are gone. The only things that survived intact are the English technical terms (LLM, VLA, RT-2, OpenVLA, π0, GR00T, Diffusion Policy, Open X-Embodiment, Sim-to-Real, etc.) and the section/table scaffolding.

**What I recovered on my own**

- ✅ The editor instructions — read straight from `scripts\prompt-instructions.txt` (clean UTF-8). I have the exact rules, front-matter format, and the three fixed `categories` (`VLA` / `피지컬 AI` / `메타`).
- ❌ The memo itself lives at `D:\blog-memo\2026-07-01-피지컬AI-기술계보.md`, which is **outside this session's sandbox** (`D:\Project\Blog`). The Read tool says permission "hasn't been granted yet," and PowerShell hard-blocked it for being outside the allowed directory.

**Why I'm stopping instead of guessing**

The memo is dense, opinionated prose (a technology-genealogy of Physical AI, with your own "한 줄 코멘트" takes on each table). I can see the *skeleton* but not a single sentence of your actual Korean text. Turning `?`-marks into a finished post would mean fabricating the specific claims and framing — exactly what the instructions ("사실을 지어내지는 마세요") and CLAUDE.md forbid.

**Fastest way forward — pick one:**

1. **Approve the Read of that one file** (or the `D:\blog-memo` folder) — then I'll generate the post immediately. *(My recommendation.)*
2. **Copy the memo into the repo**, e.g. `D:\Project\Blog\scratch-memo.md`, and I'll read it there.
3. **Paste the memo text** directly into chat (as long as your terminal sends UTF-8, not the mangled version).

Which would you like? Once I can read the real Korean, the post itself is quick.